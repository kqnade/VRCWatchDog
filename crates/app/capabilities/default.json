{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 5d skeleton: core only. shell:allow-open は意図的に外している (plan §7) — JS から shell.open は呼ばせず、Rust 側 app.shell().open() でだけ限定使用するため。",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
